import os

Import('env')

env.SharedLibrary('libtango-stub.so', ['tango-stub.cc'])

env.Program('atm.exe', ['atm.cc'], LIBPATH='#lib', LIBS='tango')
env.Program('ocean.exe', ['ocean.cc'], LIBPATH='#lib', LIBS='tango')
