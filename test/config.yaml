
grids:
    - name: ocean
      destinations:
        - name: ice
          fields: [sst]

    - name: atm
      destinations:
        - name: ice
          fields: [temp, swflux]

    - name: ice
      destinations:
        - name: ocean
          fields: [temp, swflux]
        - name: atm
          fields: [sst]
