
mappings:
    - source_grid: ocean
      destination_grid: ice
      fields: [sst]

    - source_grid: ice
      destination_grid: ocean
      fields: [temp, swflux]

    - source_grid: atm
      destination_grid: ice
      fields: [temp, swflux]

    - source_grid: ice
      destination_grid: atm
      fields: [sst]
